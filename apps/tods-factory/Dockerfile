# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tods-factory`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tods-factory`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tods-factory && \
          adduser --system -G tods-factory tods-factory

COPY dist/apps/tods-factory tods-factory/
RUN chown -R tods-factory:tods-factory .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tods-factory --omit=dev -f install

CMD [ "node", "tods-factory" ]
